---
created: 2025-09-15 13:10:36
title: memo_2025-09-15
tags:
  - dailymemo
---
←[[2025-09-14-01]]
→[[2025-09-16-01]]
### 100本ノック
#### 今日の進捗
**データ加工**
33~58 (total:33~58)
33~58 (total:33~58, 復習)

#### 気になったポイント
33 : 特定のデータの値を変える場合は`loc[3, 'age']` などで指定してから
37 : 小数点以下のまとめ方 `round()` の使い方は要注意
39 : 列同士の結合には `str.cat()` を使用
40 : 数値型を `df['age'].astype(str)` で文字型に変えたのち結合
41 : 列の削除には `df.drop('body', axis = 1)` なお行の削除なら `axis = 0` とする
42 : 列名をまとめて変更するときは `df2.columns = ['name', 'class', 'Biology', 'Physics', 'Chemistry']` で一気に指定が可能
45 : インデックス変更は `df2 = df2.rename(index={1 : 10})` が確実。
46 : `isnull()` の反対は単純に `notnull()` がある。レコード数を確認するなら `isnull().sum()`
50 : 行をシャッフルするときは `df.sample(frac=1)` で。
51 : シャッフル後インデックスを振り直すときは `reset_index()` を使用 する
52 : 重複行のカウントは `df.duplicated().value_coutns()` で数える。
53 : 大文字にする場合は`.str.upper()` 
55 : 指定の値の置換は `df['sex'] = df['sex'].replace('female','Python')` でも可。
56 : 値のうち特定のものを変換する場合に、ライブラリを使用して `re.sub('消したい文字列','','元々の文字列')` として置換ができる。
57 : `strip()` で空白削除
